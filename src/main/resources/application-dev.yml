spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/labs
    username: postgres
    password: qwerty
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8380
